# ───────────── macOS ─────────────
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# ───────────── Xcode: 빌드 산출물 ─────────────
# 소스에서 다시 만들어지는 결과물이라 git에 둘 필요 없음
build/
DerivedData/
*.app
*.ipa
*.dSYM.zip
*.dSYM

# ───────────── Xcode: 사용자별 설정 ─────────────
# 개인 IDE 상태(열어둔 파일, 브레이크포인트 등) — 공유 불필요
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# 워크스페이스 사용자 설정
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# ───────────── 패키지 매니저 ─────────────
# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (쓰는 경우)
# Pods/

# Carthage (쓰는 경우)
# Carthage/Build/

# ───────────── 기타 ─────────────
*.moved-aside
*.hmap
*.ipa
*.xcarchive
timeline.xctimeline
playground.xcworkspace
